language: cpp

compiler: gcc

sudo: required

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq sqlite3
 - sudo apt-get install build-essential
 - sudo apt-get install qt5-default
 - sudo apt-get install qtdeclarative5-dev

before_script:
  - qmake softProjector.pro
  

script: make
